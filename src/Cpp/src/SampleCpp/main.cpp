/*----------------------------------------------------------------------------------------------------------------------
    mt19937 sınıfı
----------------------------------------------------------------------------------------------------------------------*/
#include <random>
#include <iostream>

int main()
{
    std::mt19937 eng{std::random_device{}()};

    int min = 10;
    int max = 20;

    for (int i{}; i < 10; ++i)
        std::cout << eng() % (max - min) + min << '\n';
    return 0;
}